<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">
    <insert id="add" useGeneratedKeys="true" keyProperty="id">
        insert into dish (name, category_id, price, status, image, description, create_time, update_time, create_user, update_user)
        values (#{name}, #{categoryId}, #{price}, #{status}, #{image}, #{description}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>
    <update id="update" useGeneratedKeys="true" keyProperty="id">
        update dish
<set>
    <if test="name != null">
        name = #{name},
    </if>
            <if test="categoryId != null">
        category_id = #{categoryId},
    </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="image != null">
                image = #{image},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
</set>
            where id = #{id}
    </update>
    <delete id="deletebatch">
        delete from dish where id in
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="pagequery" resultType="com.sky.vo.DishVO">
        SELECT
        d.*,
        c.name as categoryName
        FROM dish d
        LEFT JOIN category c ON d.category_id = c.id
        <where>
            c.status = 1
            <if test="name != null and name != ''">
                AND d.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="categoryId != null">
                AND d.category_id = #{categoryId}
            </if>
            <if test="status != null">
                AND d.status = #{status}
            </if>

        </where>
        ORDER BY d.create_time DESC
    </select>
    <select id="findById" resultType="com.sky.vo.DishVO">
        SELECT
        d.*,
        c.name as categoryName
        FROM dish d
        LEFT JOIN category c ON d.category_id = c.id
        WHERE d.id = #{id}
    </select>
    <select id="list" resultType="com.sky.vo.DishVO">
        SELECT
        d.*,
        c.name as categoryName
        FROM dish d
        LEFT JOIN category c ON d.category_id = c.id
        WHERE category_id=#{categoryId} and c.status=1 and d.status=1
    </select>
</mapper>